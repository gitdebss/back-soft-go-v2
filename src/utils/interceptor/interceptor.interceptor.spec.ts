import { InterceptorInterceptor } from './interceptor.interceptor.js';

describe('InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorInterceptor()).toBeDefined();
  });
});
